# Browser-facing API base. Prefer the Next.js same-origin proxy.
NEXT_PUBLIC_API_BASE_URL=/backend-api

# Server-side rewrite target for /backend-api/* (see next.config.mjs)
API_INTERNAL_URL=http://localhost:4000/api
